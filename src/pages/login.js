import React from 'react';
import UserLogin from '../components/UserLogin';

const LoginPage = () => {
    return (
        <div>
            < UserLogin />
        </div>
    );
}

export default LoginPage;